if status is-interactive
    # Commands to run in interactive sessions can go here
end

alias grep="grep --color=auto"
alias ls="ls --color=auto"
alias l="ls -al --color=auto"
alias bye="exit"
alias ex=bye

# Fish shell can adopt .bashrc out of the box! So I didn't push back them in this config.
# Copy the .bashrc over your home directory which si recommended.
